// Extension UI components are no longer proxied through the SDK.
// Tier 2 (installed) extensions should bundle their own UI components
// or rely on a shared CSS design system provided by the host.
